# Core. numpy/pandas/scipy/matplotlib/streamlit are assumed present system-wide;
# create the venv with --system-site-packages to inherit them.
numpy>=1.26
pandas>=2.0
scipy>=1.11          # scipy.optimize.milp, HiGHS backend
matplotlib>=3.6
lightgbm>=4.0        # quantile regression
pyarrow>=14          # parquet
streamlit>=1.30      # replay UI
pytest>=7.0
shap>=0.44           # eval/interpret.py, tree explainer on the boosters
torch>=2.0           # the neural quantile benchmark in forecast/baselines.py
holidays>=0.40       # per-country calendars for the comparative study; a fixed
                     # list cannot express India's lunisolar holidays
# highspy is not required (scipy vendors HiGHS) but is useful for direct solver work
